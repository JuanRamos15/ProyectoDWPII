{{> navbarRoot}}
<h1>Listado de Usuarios</h1>
<form action="/root/userList" method="GET" style="display: flex; gap: 10px;">
  <input type="text" name="query" placeholder="Buscar libros por Usuarios por Nombre o ID" style="padding: 10px; flex-grow: 1;">
  <button type="submit" style="padding: 10px 20px; background-color: #007BFF; color: white; border: none; cursor: pointer;">Buscar</button>
</form>
<div class="row">
  {{#each users}}
  <div class="col s12">
    <div class="card teal darken-4">
      <div class="card-content teal-text text-lighten-5">
        <span class="card-title">Usuario</span>
        <p>Nombre:{{firstName}}</p>
        <p>Apellido:{{lastname}}</p>
        <p>Numero de control: {{studentId}}</p>
        <p>Carrera: {{major}}</p>
        <p>Email: {{mail}}</p>
      </div>
      <div class="card-action">
        <a href="/root/modifyUser/{{_id}}">
          <i class="material-icons"
            style = "display: inline-flex; vertical-align: sub;">edit</i>
            Editar Usuario
        </a>
        <a href="javascript: deleteUser('{{_id}}')" class="inline">
          <i class="material-icons"
            style = "display: inline-flex; vertical-align: sub;">
            delete</i>
            Borrar Usuario
        </a>
      </div>
    </div>
    </div>
  </div>
  {{else}}
    <p>No hay usuarios que listar.</p>
  {{/each}}